#include <stdio.h>

/**
 *main - prints all single digit numbers of base 10 starting from o
 *Return: Always (Success)
 */

int main(void)
{
	n = 0;
	while (n <= 9)
	{
		putchar(n);
		n++;
	}
	putchar('\n');

	return (0);
}
